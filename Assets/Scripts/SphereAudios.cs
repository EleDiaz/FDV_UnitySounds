using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// Tarea:  Implementar un script en el que el cubo-player al colisionar con las esferas active un sonido.

// Tarea: Modificar el script anterior para que según la velocidad a la que se impacte, el cubo lance un sonido más fuerte o más débil.
public class SphereAudios : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
